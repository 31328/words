package com.words.spring.services;

public interface LetterService {
    String generateRandomGreekLetters(int count);
    String generateRandomEnglishLetters(int count);
    String generateRandomGermanLetters(int count);
    String generateRandomItalianLetters(int count);
    String generateRandomSwedishLetters(int count);
    String generateRandomAlbanianLetters(int count);
    String generateRandomMongoliaLetters(int count);
    String generateRandomPolishLetters(int count);
    String generateRandomFrenchLetters(int count);
    String generateRandomSpanishLetters(int count);
    String generateRandomBulgarianLetters(int count);
    String generateRandomSerbianCyrillicLetters(int count);
    String generateRandomSerbianLatinLetters(int count);
    String generateRandomRussianLetters(int count);
}
